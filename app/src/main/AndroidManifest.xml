<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.bentengbuahnaga.MangementApp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.dapur.CekBahanActivity"></activity>
        <activity android:name=".activity.dapur.MenuKoki" />
        <activity android:name=".activity.owner.OwnerActivity" />
        <activity android:name=".activity.pelayan.PelayanActivity" />
        <activity android:name=".activity.dapur.DaftarMasakActivity" />
        <activity android:name=".activity.dapur.BerandaKokiActivity" />

        <service android:name=".service.Service">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".activity.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>